# README

Retired artifacts kept for provenance only. Do not build against these.

- `green-pm-tokens-v2-2026-05-21.css` — superseded v2 token set (old variable
  names: `--surface-alt`, `--measure-*`, `--container-*`). Replaced by the
  v3.0.0 set at `../styles/green-pm-tokens.css`.
- `README-claude-design-handoff-2026-05-21.md` — earlier Claude Design handoff
  README that referenced a folder layout (`css/`, `colors/`) never adopted.
  Replaced by `../README.md`.

This folder is `_`-prefixed and ignored by git.
