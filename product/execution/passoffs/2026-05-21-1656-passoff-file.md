# Summary

Restored the IA evaluator as a focused versioned project Git hook after the prior hook changes were reverted in cleanup PR #25. Added `.githooks/pre-commit`, configured the local checkout with `core.hooksPath=.githooks`, updated `.claude/evaluate-ia-change.mjs` to allow `.githooks/` as a canonical top-level project zone, and updated `.claude/information-architecture.md` to document project-hook activation instead of a suggested local `.git/hooks` snippet.

# Files changed

- Created: `.githooks/pre-commit`
- Updated: `.claude/evaluate-ia-change.mjs`
- Updated: `.claude/information-architecture.md`
- Created: `docs/passoffs/2026-05-21-1656-passoff-file.md`

# Key decisions

- Made the hook a committed project artifact under `.githooks/` so the wrapper can be reviewed and versioned.
- Kept the policy logic in `.claude/evaluate-ia-change.mjs`; the Git hook only delegates to it.
- Configured this checkout with `git config core.hooksPath .githooks`; future clones must run the same command once because Git local config is not versioned.
- Kept this change focused on the hook restoration and did not modify design-system content.

# Risks and open questions

- Git does not automatically enable committed hooks in fresh clones; developers and agents must run `git config core.hooksPath .githooks` once per checkout unless a future bootstrap script is added.
- The IA evaluator remains deterministic and path/frontmatter/index oriented; it does not replace content review.

# State

- Branch: `codex/project-ia-hook`
- Commit SHA: `4a6db27` before this PR URL follow-up
- PR URL: `https://github.com/klappe-pm/green-lappe-properties/pull/26`

# Next actions

1. Review and merge the project-hook PR if it is mergeable and has no failing required checks.
2. Sync local `main` after merge.
3. For any fresh clone, run `git config core.hooksPath .githooks` to activate the committed hook.
4. Continue using `docs/design-system/docs/31-token-atomic-execution.md` as the canonical plan for parallel token-first design-system work.
5. Continue respecting prior launch constraints before any public domain, DNS, staging, production, or legal-adjacent work.

# Do not do

- Do not treat design-system, launch, domain, trademark, registrar, storage, or runbook materials as legal, trademark, licensing, tax, insurance, privacy, cybersecurity, or records-retention advice.
- Do not buy domains, configure DNS, connect hosting, or publish production content without explicit approval and required account-owner action.
- Do not commit registrar credentials, receipts, personal registrant data, payment details, recovery codes, counsel memos, license documents, or sensitive screenshots to this repository.
- Do not publish public sales or property-management service language before licensing/counsel gates clear.
- Do not remove large sections of design-system content without explicit approval.
