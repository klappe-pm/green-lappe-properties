# Summary

Aligned Green Lappe documentation, guidance, and archive indexes with the
current recursive file layout. The repo README, IA policy, and IA evaluator now
describe `docs/uxr/`, `docs/uxd/design-system/`,
`docs/strategies/market/`, and `docs/_archive/` as the active structure;
active UXR/backlog/launch references no longer point at removed
`docs/research/`, `docs/design/`, `docs/fund/`, or `docs/opportunities/`
paths. Archive README files now present retired opportunity and deck materials
as historical reference rather than active deliverables.

# Files changed

- Modified: `.claude/evaluate-ia-change.mjs`,
  `.claude/information-architecture.md`, `README.md`.
- Modified: `docs/backlog/backlog.md`, `docs/backlog/codex-index-model.md`.
- Modified: `docs/launch/README.md` and
  `docs/launch/plans/2026-05-20-business-launch-project-plan.md`.
- Modified: `docs/_archive/README.md`,
  `docs/_archive/decks/README.md`.
- Modified relocated UXR/design-system docs with stale path references.
- Moved legacy research corpus from `docs/research/` to `docs/uxr/`.
- Moved design-system package from `docs/design-system/` to
  `docs/uxd/design-system/`.
- Moved market strategy files to `docs/strategies/market/`.
- Moved retired opportunity and deck materials to `docs/_archive/` and
  `docs/backlog/`.
- Created: this passoff.

# Key decisions

- Preserved legacy UXR and market-strategy filenames during the one-time IA
  relocation instead of renaming every historical research file.
- Treated the old opportunity package as retired archival material, not the
  active partner-readiness source of truth.
- Kept launch readiness and public-launch tasks blocked on owner, broker,
  counsel, account-owner, vendor, or market-conversation input.

# Risks and open questions

- The archived corpus indexer under `docs/_archive/decks/_tools/` still reflects
  the retired `docs/opportunities/` classifier and should not be used until it
  is rebuilt or explicitly retired.
- Empty local-only folders and ignored `.DS_Store` files may still exist on disk
  but are not part of the tracked IA.
- Launch readiness and operations work remain blocked on user-supplied external
  decisions and evidence.

# Lessons learned

- IA evaluators need explicit migration exceptions when a repo intentionally
  relocates a legacy research corpus. Otherwise deterministic filename and
  frontmatter checks can block the IA cleanup they are meant to protect.

# State

- Branch: `codex/recursive-docs-alignment`
- Commit SHA: pending
- PR URL: pending

# Next actions

1. Run `node "$(git rev-parse --show-toplevel)/.claude/evaluate-ia-change.mjs" --staged`
   after staging the IA move.
2. Decide whether to rebuild or retire the archived opportunity corpus indexer.
3. After merge, sync local `main` and delete the merged feature branch.

# Do not do

- Do not delete unmerged local or remote branches without explicit approval.
- Do not treat UXD diagrams as legal, licensing, insurance, trust-accounting,
  privacy, cybersecurity, tax, records-retention, or public-launch approval.
- Do not buy domains, configure DNS, connect hosting, publish production
  content, or commit credentials, receipts, personal registrant data, payment
  details, recovery codes, counsel memos, license documents, or sensitive
  screenshots.
- Do not use `--no-verify`, force-push, reset, or bypass branch protections.
