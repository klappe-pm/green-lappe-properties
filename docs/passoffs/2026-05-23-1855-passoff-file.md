# Summary

Committed the session-data generated LLM breadcrumb pointers for Green Lappe
Properties. The root pointer gives future LLM sessions a low-token route back
to this project root, local passoff history, and the canonical session-data
breadcrumb implementation. The history-folder pointer uses the reserved
`0000-00-00-0000-llm-breadcrumbs.md` name so newest-passoff discovery still
sorts to the newest real passoff.

Updated the repository README and IA allowlist so `LLM-BREADCRUMBS.md` is an
intentional durable top-level file rather than an unclassified artifact.

# Files changed

- `.claude/information-architecture.md`
- `README.md`
- `LLM-BREADCRUMBS.md`
- `docs/passoffs/0000-00-00-0000-llm-breadcrumbs.md`
- `docs/passoffs/2026-05-23-1855-passoff-file.md`

# Key decisions

- Tracked the breadcrumb pointers in the owning repository because they are
  project navigation metadata generated by the merged session-data breadcrumb
  workflow.
- Updated the IA allowlist in the same change because Green Lappe validates
  top-level durable files explicitly.
- Kept the history-folder pointer filename at the low-sorting `0000-...`
  prefix so it cannot shadow real passoffs.

# Risks and open questions

- Breadcrumb pointers are summaries and navigation aids, not source evidence.
  Future agents should still rely on source passoffs, transcripts, and report
  data when accuracy matters.
- Launch readiness and operations work remain blocked on user-supplied owner,
  broker, counsel, account-owner, vendor, or market-conversation input.

# Lessons learned

- Generated navigation files still need to satisfy project IA rules; treating
  them as durable files keeps future cleanup deterministic.

# State

- Branch: `codex/llm-breadcrumb-pointers`
- Commit SHA: pending
- PR URL: pending
- Verification:
  - `node "$(git rev-parse --show-toplevel)/.claude/evaluate-ia-change.mjs" --staged`: passed for 6 changed files
  - `git diff --check --cached`: passed

# Next actions

1. Push this branch, open a PR, record the PR URL, and merge after checks pass.
2. Keep launch/legal/ops items blocked until the user supplies the required
   external decisions and evidence.

# Do not do

- Do not move `LLM-BREADCRUMBS.md` directly into `docs/passoffs/`.
- Do not treat breadcrumb pointers as legal, licensing, insurance,
  trust-accounting, privacy, cybersecurity, tax, records-retention, or
  public-launch approval.
- Do not buy domains, configure DNS, connect hosting, publish production
  content, or commit credentials, receipts, personal registrant data, payment
  details, recovery codes, counsel memos, license documents, or sensitive
  screenshots.
