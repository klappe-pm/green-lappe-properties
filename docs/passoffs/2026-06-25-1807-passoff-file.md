---
domain: real-estate
category: green-lappe
sub-category: infrastructure
date-created: 2026-06-25
date-revised: 2026-06-25
type: passoff
status: DONE
aliases: []
tags: []
---

# 2026-06-25-1807-passoff-file

## Summary

Wired the green-pm-site Astro app to deploy to GitHub Pages via a new GitHub Actions workflow (`deploy-pages.yml`). Added `BASE_PATH` env var support to `astro.config.mjs` so the site builds correctly under the `/green-lappe-properties` subpath. Copied `product/branding/brand-book/*.html` into the build output so both HTML files are accessible at the Pages URL. Merged PR #66, switched GitHub Pages source from legacy to Actions mode, and cleaned up worktrees. Committed pending prior-session archive frontmatter changes as PR #67 (open, awaiting CI).

## Files changed

**Created**
- `.github/workflows/deploy-pages.yml` — build + deploy workflow for GitHub Pages

**Modified**
- `src/green-pm-site/astro.config.mjs` — added `BASE_PATH` env var and `base` option
- `product/_recovered/_archive/*.md` (11 files) — added `status: ARCHIVED` frontmatter (PR #67)

## Key decisions

- Used GitHub Actions Pages source (not legacy branch-root), which requires `build_type=workflow` in the Pages API — switched via `gh api` after merge.
- Set `base: BASE_PATH` in Astro config; defaults to `''` for production (`greenpmpnw.com`) so no existing links break.
- Copied brand-book HTML into `dist/brand-book/` during the workflow (not as a public/ asset) to keep it out of the Astro build graph while still serving it at a stable Pages URL.
- Used squash merge for PR #66 per project convention.
- Skipped CLAUDE.md Git-section update — a hook reverted it; left as-is.

## Risks and open questions

- PR #67 (archive frontmatter) is open and has not been reviewed or merged. CI must pass before landing.
- The deploy-pages workflow fires on next push to `main` touching `src/green-pm-site/**`. First live deploy will happen when PR #67 merges (if it touches those paths — it does not, so the deploy will not re-trigger until the next site push).
- Three stashes exist on `main` from prior sessions (2026-06-22 and 2026-06-17). These contain dirty-tree snapshots and should be reviewed and dropped when safe.
- Two branches with `[gone]` upstream remain in local refs: `chore/tracking-guarantee-automation` and `feat/github-pages-deploy`. These can be pruned.

## Workspace sweep

**Auto-resolved**
- Staged and committed `product/_recovered/_archive/` frontmatter changes on `chore/session-cleanup` worktree (PR #67).
- Removed `.DS_Store` files from repo filesystem (were gitignored, not tracked).

**Needs user action**
- `stash@{0}` — daemon-dirt 2026-06-22: `_archive/log` stamps. Review with `git stash show -p stash@{0}` and drop if stale: `git stash drop stash@{0}`.
- `stash@{1}` — pre-sync-stash 2026-06-22: README link fixes + ARCHIVED sweep + lockfile regen. Review with `git stash show -p stash@{1}`. Drop or pop and commit if still needed.
- `stash@{2}` — resume-20260617: 301 deletes + 2 edits + untracked. Almost certainly stale. Drop with `git stash drop stash@{2}` after verifying.
- `chore/tracking-guarantee-automation` local branch points to a gone upstream — prune with `git branch -d chore/tracking-guarantee-automation`.
- `feat/github-pages-deploy` local branch points to a gone upstream (merged PR #66) — prune with `git branch -d feat/github-pages-deploy`.
- `green-lappe-chore` worktree (`chore/session-cleanup`) is still live — once PR #67 merges, remove it: `git worktree remove ../green-lappe-chore && git branch -d chore/session-cleanup`.

## Lessons learned

None this session.

## State

- Branch: `main` (primary checkout at `/Users/kevinlappe/Projects/ppi-platform/green-lappe-properties`)
- Commit: `fe01970` (main tip after PR #66 merge)
- PR #66: merged — `https://github.com/klappe-pm/green-lappe-properties/pull/66`
- PR #67: open — `https://github.com/klappe-pm/green-lappe-properties/pull/67`
- Passoff version: v1

## Next actions

1. Merge PR #67 once CI passes: `gh pr merge 67 --repo klappe-pm/green-lappe-properties --squash --delete-branch`
2. Remove `green-lappe-chore` worktree and local branch: `git worktree remove ../green-lappe-chore && git branch -d chore/session-cleanup`
3. Prune gone-upstream local branches: `git branch -d chore/tracking-guarantee-automation feat/github-pages-deploy`
4. Review and drop the three stashes (see Workspace sweep above).
5. Verify the Pages deploy at `https://klappe-pm.github.io/green-lappe-properties/` and brand book at `https://klappe-pm.github.io/green-lappe-properties/brand-book/gpm-brand-book.html`.

## Do not do

- Do not merge PR #67 if CI is red.
- Do not push commits directly to `main` — branch-guard hook blocks it.
- Do not configure DNS or connect the custom `greenpmpnw.com` domain without user direction.
- Do not drop the stashes without reviewing their contents first.
