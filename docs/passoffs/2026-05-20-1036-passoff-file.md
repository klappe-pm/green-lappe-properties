# Summary

Settled the non-Obsidian portion of the Green Lappe dirty worktree for Phase 1.
The deleted competitor dataset artifacts and `vault/templates/base-template.md`
were confirmed intentional by Kevin, so this branch records those deletions
alongside the moved competitor markdown, new insights/backlog docs, rewritten
demographics insight report, and exported deck files. `.obsidian` config churn
remains intentionally unstaged for a separate tracked-vs-local decision.

# Files changed

- `docs/backlog/` - replaces the old backlog path with current backlog and
  backlinks companion files.
- `docs/insights/` - adds the competitor research insight note.
- `docs/research/competitors/` - moves competitor research markdown into the
  current competitors location.
- `docs/research/datasets/competitors/` - removes obsolete dataset exports
  confirmed as intentionally deleted.
- `docs/research/market/` - removes markdown files moved into competitors or
  insights.
- `docs/research/reports/Market Research Insights - Demographics.md` - records
  the rewritten insight report.
- Root `*.pptx` deck exports - adds current deck artifacts for review.
- `vault/templates/base-template.md` - removes the deleted vault template.
- `docs/passoffs/2026-05-20-1036-passoff-file.md` - records this session.

# Key decisions

- Treated the dataset and template deletions as intentional after explicit user
  confirmation.
- Kept `.obsidian/appearance.json` and
  `.obsidian/plugins/obsidian-linter/data.json` out of this commit because they
  change editor/theme/linter behavior rather than project content.
- Did not discard or revert any user work.

# Risks and open questions

- `.obsidian` config remains dirty and still needs a decision: commit as shared
  vault behavior, revert, or move toward local-only config.
- Multiple root deck exports are committed for preservation, but the canonical
  deck still needs to be selected in a later cleanup.
- README and docs indexes may still reference old `docs/research/market/` or
  `docs/research/datasets/` paths; Phase 2/3 should update governance and
  project docs after the dirty tree is settled.
- Strict markdown audit is still expected to fail until the Phase 6 markdown
  policy decision is made.

# State

- Branch: `codex/phase1-green-triage`
- Commit SHA: `eda7d7f` before this passoff commit
- PR URL: pending

# Next actions

1. Commit this Green Lappe non-Obsidian triage set.
2. Push `codex/phase1-green-triage`.
3. Open a PR against `klappe-pm/green-lappe-properties:main`.
4. Merge after checks pass and no branch protection or review blockers remain.
5. Decide `.obsidian` config treatment before considering Green Lappe fully
   clean for Phase 1.

# Do not do

- Do not discard the remaining `.obsidian` changes without explicit approval.
- Do not delete or remove duplicate deck exports until the canonical deck is
  selected.
- Do not start Phase 2 Green Lappe policy rewrites until the `.obsidian` state
  is settled or explicitly parked.
