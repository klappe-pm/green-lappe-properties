# Summary

Continued from the 17:00 passoff after confirming PR #27 had already merged and local `main` matched `origin/main`. Aligned the project IA passoff contract and evaluator with the global passoff policy by requiring the `Lessons learned` section. Fixed the remaining design-system graph drift by converting a rejected-example Obsidian wikilink into plain path-like text, and updated the frontmatter schema example to use explicit `aliases: []` and `tags: []` arrays.

# Files changed

- Updated: `.claude/information-architecture.md`
- Updated: `.claude/evaluate-ia-change.mjs`
- Updated: `docs/design-system/docs/00-index.md`
- Updated: `docs/design-system/docs/96-numbering-convention.md`
- Created: `docs/passoffs/2026-05-21-1710-passoff-file.md`

# Key decisions

- Treated the global passoff policy as authoritative for the required section list, adding `Lessons learned` to the project IA text and evaluator.
- Left the pre-existing Tailwind ESM module-type warning unresolved because the config still imports successfully and the warning points outside the tracked repo metadata.
- Kept the design-system change non-semantic: the rejected folder-link example still communicates the same rationale without creating a false unresolved wikilink.

# Risks and open questions

- The launch and operations backlog still contains business tasks that require owner, broker, counsel, account-owner, or market-conversation input before repo work can close them.
- Tailwind config import succeeds but Node continues to report the pre-existing module-type warning because the nearest package metadata does not declare ESM.
- No product implementation, public launch, domain purchase, DNS change, or account configuration was performed.

# Lessons learned

- Keep project-local IA checks synchronized with `~/.claude/PASSOFF.md`; otherwise the hook can approve passoffs that fail the cross-agent contract.
- Documentation examples that use `[[...]]` are parsed as real Obsidian links by verification, so hypothetical or rejected targets should be plain text or code instead.

# State

- Branch: `codex/passoff-policy-design-system-verification`
- Commit SHA: `pending`
- PR URL: `pending`

# Next actions

1. Merge this branch only if GitHub reports it clean and there are no failing required checks, unresolved review blockers, draft status, or branch protection warnings.
2. Sync local `main` after merge.
3. Continue using `docs/design-system/docs/31-token-atomic-execution.md` as the canonical plan for any future token-first design-system work.
4. Treat active launch and operations backlog items as blocked on explicit owner or professional input unless the user supplies the missing decisions.

# Do not do

- Do not treat design-system, launch, domain, trademark, registrar, storage, or runbook materials as legal, trademark, licensing, tax, insurance, privacy, cybersecurity, or records-retention advice.
- Do not buy domains, configure DNS, connect hosting, or publish production content without explicit approval and required account-owner action.
- Do not commit registrar credentials, receipts, personal registrant data, payment details, recovery codes, counsel memos, license documents, or sensitive screenshots to this repository.
- Do not publish public sales or property-management service language before licensing/counsel gates clear.
- Do not remove large sections of design-system content without explicit approval.
