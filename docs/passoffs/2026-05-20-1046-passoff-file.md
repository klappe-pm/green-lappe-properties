# Summary

Updated the root README repo layout section from a plain code-block tree into a linked folder pointer table. Each listed folder now has a Markdown link and a concise summary of what belongs there. Verified every linked folder in the table exists locally before committing.

# Files changed

- Updated `README.md`
- Created `docs/passoffs/2026-05-20-1046-passoff-file.md`

# Key decisions

- Kept the pointer table focused on folders, not individual files.
- Included both current docs-native workflow folders and legacy `project-management/` folders.
- Preserved archived deck variants and only pointed to their archive folder.

# Risks and open questions

- Historical passoffs still mention older paths because they record prior state.
- The canonical partner deck is still not selected; deck variants remain archived under `docs/opportunities/decks/`.

# State

- Branch: `codex/readme-pointer-table`
- Commit SHA: pending
- PR URL: pending

# Next actions

1. Commit the README pointer table update.
2. Push `codex/readme-pointer-table`.
3. Open a PR against `main`.
4. Merge after GitHub reports a clean PR with no checks or review blockers.
5. Prune stale branch refs and verify local `main` is clean and synced with `origin/main`.

# Do not do

- Do not delete archived deck variants until the canonical deck is selected.
- Do not rewrite historical passoffs to remove old-path references.
- Do not use broad TAM/SAM/SOM as proof-sprint operating capacity.
