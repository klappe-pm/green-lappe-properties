# Summary

Applied the project-wide Obsidian ignore policy to Green Lappe Properties. The
repo now ignores the entire `.obsidian/` directory, and previously tracked
Obsidian configuration/plugin/theme files were removed from the git index while
remaining on disk locally. This resolves the lingering `.obsidian` dirty state
without deleting local editor configuration.

# Files changed

- `.gitignore` - replaces individual Obsidian workspace ignores with
  `.obsidian/`.
- `.obsidian/**` - removed from git tracking with `git rm --cached`; local
  files remain present and ignored.
- `docs/passoffs/2026-05-20-1039-passoff-file.md` - records this session.

# Key decisions

- Treated the user's instruction as requiring tracked Obsidian files to be
  untracked, not merely ignored for future additions.
- Used `git rm --cached` so no local files were deleted.

# Risks and open questions

- Obsidian plugin/theme settings will no longer travel with the repo.
- Existing clones will retain local `.obsidian` files after pulling, but they
  will be ignored going forward.

# State

- Branch: `codex/ignore-obsidian-config`
- Commit SHA: pending
- PR URL: pending

# Next actions

1. Commit and push this ignore/untrack change.
2. Open and merge the PR after checks or mergeability pass.
3. Continue applying `.obsidian/` ignores to the remaining project repos.

# Do not do

- Do not delete local `.obsidian` files from disk.
- Do not stage unrelated project work in other repos.
