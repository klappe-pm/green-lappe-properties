# Summary

Created a generalized LLM instruction prompt at
`scaffolding/docs/prompt-new-project-scaffolding.md`. The new file turns the
prior Lapsauskas-specific scaffold prompt into a reusable, parameterized prompt
for bootstrapping a new related project repository from a reference repository.
It uses bracketed placeholders for target repo, target project, reference repo,
and target domain, and it instructs the receiving LLM to copy process,
information architecture, validation, passoff, and safety patterns rather than
business-specific facts.

Updated the root `README.md` to document `scaffolding/` as the home for
cross-project LLM instructions and templates. No files were written to
`/Users/kevinlappe/Projects/lapsauskas-properties`.

# Files changed

- Updated `README.md` with the new `scaffolding/` repo-layout entry and
  workflow note for `scaffolding/docs/`.
- Created `scaffolding/docs/prompt-new-project-scaffolding.md`.
- Created `docs/passoffs/2026-06-07-1659-passoff-file.md` (this passoff).

# Key decisions

- Used `scaffolding/docs/` because the user requested that exact path and the
  file is cross-project scaffolding material, not an in-corpus
  `docs/prompts/` prompt.
- Kept the prompt generic and parameterized instead of making another
  Lapsauskas-only prompt.
- Preserved the prior safety boundary: reference-project specifics such as
  names, domains, pricing, geography, legal conclusions, licensing assumptions,
  vendor choices, brand systems, rankings, public approvals, and sensitive
  evidence must not be copied unless the user explicitly confirms them.
- Did not stage ignored `.claude/` files. The local ignored IA evaluator and IA
  note were updated so this checkout can validate the new `scaffolding/` path,
  but those local files remain outside the tracked diff.

# Risks and open questions

- Because `.claude/` is ignored in this checkout, future clones may need local
  IA allowlist alignment before validating edits under `scaffolding/`.
- The prompt is a reusable instruction payload; it does not scaffold any target
  repo by itself.
- The root README now documents `scaffolding/` as a durable top-level zone, but
  the tracked IA evaluator is not part of the repo after prior untracking.

# Lessons learned

- The project has two useful prompt homes now: `docs/prompts/` for prompts that
  live inside this project's corpus, and `scaffolding/docs/` for cross-project
  LLM instructions and templates.

# State

- Branch: `codex/new-project-scaffolding-prompt`
- Base commit before this session's commit: `c91cb26`
- PR URL: https://github.com/klappe-pm/green-lappe-properties/pull/46
- Validation: `node /Users/kevinlappe/Projects/green-lappe-properties/.claude/evaluate-ia-change.mjs --staged` passed for 3 changed files after this passoff was staged.

# Next actions

1. Merge only if GitHub reports the PR is mergeable with no failing required
   checks, unresolved review threads, draft status, or branch-protection
   warnings.
2. Sync local `main` after merge.

# Do not do

- Do not write directly into `/Users/kevinlappe/Projects/lapsauskas-properties`
  as part of this instruction-file task.
- Do not copy Green Property Management business facts, brand decisions,
  licensing assumptions, launch approvals, ZIP rankings, pricing, or operator
  details into another project without explicit user confirmation.
- Do not stage ignored local `.claude/` tooling unless the user explicitly asks
  to re-track that local IA machinery.
- Do not invent legal, licensing, brokerage, insurance, trust-accounting,
  account-owner, vendor, counsel, financing, domain, or public-launch decisions.
- Do not commit sensitive evidence, credentials, registrar data, legal memos,
  insurance documents, license documents, payment details, recovery codes,
  personal registrant data, or sensitive screenshots.
