# Summary

Session-close passoff for the website-build kickoff. This session reviewed the
repo for blockers to building/delivering the live Green Property Management
website, made two governing decisions with the user, started a partial in-repo
site scaffold, opened PR #48, and subscribed to that PR's activity. No new code
or docs were produced after the prior passoff
(`2026-06-12-1342-passoff-file.md`); the working tree is clean. The user is
moving to a local session to continue.

This passoff supersedes the State section of `2026-06-12-1342-passoff-file.md`
(the PR is now open) but defers to that file for the full, detailed Next-actions
list, which is still the authoritative work queue.

# Files changed

None this increment (working tree clean since the prior passoff). State of the
branch as shipped:

- `site/` partial scaffold: `package.json`, `astro.config.mjs`,
  `tailwind.config.js` (preflight disabled so design-system `base.css` is the
  only reset), canonical tokens copied into `src/styles/`, WIP `site/README.md`.
- `docs/passoffs/2026-06-12-1342-passoff-file.md` — full blocker analysis,
  decisions, and detailed Next actions.
- This file — session-close status.

# Key decisions

- **Canonical brand/domain:** Green Property Management / "Green PM" /
  `greenpmpnw.com` (design system v3 wins over the older `greenlappe.com` note).
- **Repo strategy:** build the website in THIS repo under `site/`, organized
  around features, until scale justifies splitting into separate repos.
- **PR opened ready (non-draft):** #48.
- **Stayed subscribed to PR #48** activity for review comments and CI.
- Did not re-track ignored `.claude/` IA tooling; did not invent any blocked
  decision; design tokens copied (not re-authored) with the design system as the
  single source of truth.

# Risks and open questions

- `site/` is **not yet buildable** (no `src/pages/` route or base layout); deps
  were never installed and no build was run.
- IA validation (`.claude/evaluate-ia-change.mjs`) could not run — `.claude/` is
  absent from fresh clones; changes were not validated by the evaluator.
- ~20 design-system TBDs/contradictions remain (screening provider, Rentvine vs
  Postmark email sender, `page.sections` fields, renter-product surface tokens,
  blog pagination query-string vs path, etc.) and must be resolved before
  feature specs are final.
- The two older website plans still reference `greenlappe.com`/open stack and
  need a "superseded by" banner.
- `send_later`/`gh` are unavailable in this session, so no automated hourly
  self-check-in could be armed for PR #48 merge/new-push transitions (webhooks
  still wake the session for review comments and CI). The local session is now
  the primary driver, so this is acceptable.

# Lessons learned

- The top blocker was governance, not code: two canonical docs disagreed on
  brand name and domain. Resolving that with the user before writing the spec/
  test suite avoided building against the wrong brand.
- This environment can reach npm (`npm ping` ok, Node v22.22.2), so a future
  session can install + build to verify the scaffold.

# State

- Branch: `claude/repo-review-blockers-ncsvne` (pushed; tracking origin).
- Working tree: clean.
- Head commit: `67c5ba6`.
- PR: https://github.com/klappe-pm/green-lappe-properties/pull/48 (open, ready).
- CI: none configured yet (no workflow committed) — nothing to fix.
- Review comments: none.
- Validation: NOT run (`.claude/` absent).
- Build: NOT run (`site/` partial, non-buildable).

# Next actions

Authoritative, detailed queue lives in
`docs/passoffs/2026-06-12-1342-passoff-file.md` Next actions. Headline order:

1. Update pointer files: `CLAUDE.md` (add Product/Website-build section,
   canonical brand/domain/stack, `site/` workflow, designate global Claude
   config `~/.claude/` precedence, note the absent `.claude/` IA tooling),
   `README.md` (add `site/`, `docs/features/`, `docs/roadmap/` zones; status =
   docs corpus + active product build; fix canonical brand/domain),
   `LLM-BREADCRUMBS.md` (add `site/`, `docs/features/` pointers).
2. Finish runnable scaffold: `BaseLayout.astro` (+ `global.css` importing tokens
   → modes → base, then `@tailwind`), `pages/index.astro`, `pages/404.astro`,
   `config/{site,brand}.ts`, one feature slice, a Vitest smoke test; then
   `npm install && npm run build && npm test`.
3. CI: `.github/workflows/site-ci.yml` (install, `astro check`, build, vitest;
   Lighthouse later).
4. Documentation suite: consolidated `docs/roadmap/` build/test/delivery plan
   (mark old website plans superseded), `docs/features/` index + agentic
   development design + spec template + 2-3 seeded feature specs, and the test
   plan tied to the design-system performance budgets + QA gates.
5. Update `scaffolding/docs/prompt-new-project-scaffolding.md` for a
   product-code/feature-organized variant and the new zones.
6. Resolve or explicitly flag the ~20 design-system TBDs before finalizing
   specs.

# Do not do

- Do not treat `site/` as a working app until a base layout + homepage exist and
  `astro build` succeeds.
- Do not change the canonical decision (Green PM / `greenpmpnw.com`; locked
  design-system stack).
- Do not re-track the ignored `.claude/` IA tooling without explicit instruction.
- Do not invent blocked decisions (counsel language, licensing/firm status,
  domain purchase, screening provider, owner payout config, public-launch
  approval) — leave as `Blocked`/`TBD`.
- Do not publish public production content or wire forms to live inboxes/CRMs;
  internal/staging only, `noindex` on non-production, no real PII in test data.
- Do not use `--no-verify`, force-push, or bypass branch protections.
