# Summary

Renamed active project-facing documentation from Green Lappe / Green Lappe Properties to Green Property Management, excluding historical archives and prior passoffs. Updated active frontmatter domain values to `green-property-management`, renamed active Green Lappe-slugged research/financial artifacts, and renamed the launch task repository from the TaskForge draft package into a Green Property Management TaskNotes repository.

Pivoted the launch task package toward the Obsidian TaskNotes plugin as the durable task manager and the Tasks plugin as the live reporting layer. The package now defines a project note, delivery hierarchy, status tags, plugin roles, session-data expectations, and a global workflow pattern that can be applied per project scope.

This passoff was created because the user issued `/pass` before the individual TaskNotes task note files were created.

# Files changed

- Updated active repo docs, launch docs, GTM docs, UXD/UXR docs, and metric notes to use Green Property Management naming and `green-property-management` domain/tag language.
- Renamed `docs/launch/projects/2026-05-23-green-lappe-taskforge-repository/` into `docs/launch/projects/2026-05-24-green-property-management-tasknotes-repository/`.
- Reworked the task repository index, project note, workflow note, draft task list, and open-questions note under the new TaskNotes repository folder.
- Renamed active artifacts:
  - `docs/financial/green-property-management-market-entry-financial-model.xlsx`
  - `docs/uxr/green-property-management-market-deep-dive.md`
  - `docs/uxr/reports/effort-6/green-property-management-final-dataset.csv`
- Updated script and prompt references that pointed to the renamed effort-6 dataset filename.
- Removed local `.DS_Store` metadata before staging.

# Key decisions

- The project name is exactly `Green Property Management`.
- TaskNotes is the durable task system: one Markdown note per durable task, with project links, dependencies, decisions, verification gates, session data, and evidence pointers.
- The Tasks plugin is the live query/reporting layer over TaskNotes-compatible task data.
- Status is tag-first and portable across TaskNotes, Tasks, Dataview, search, and LLM automation:
  - `#status/planned`
  - `#status/open`
  - `#status/active`
  - `#status/blocked`
  - `#status/review`
  - `#status/done`
  - `#status/canceled`
- TaskNotes `status` frontmatter should mirror the canonical status tag.
- Historical archives and prior passoffs intentionally retain old names as historical records.
- Absolute local repo paths still contain `/Users/kevinlappe/Projects/green-lappe-properties` because the repo directory itself has not been renamed.

# Risks and open questions

- The project note links to intended TaskNotes task files under `tasks/`, but those individual task note files do not exist yet.
- The launch target remains 2026-06-15, but the launch is externally blocked until owner, broker, counsel, account-owner, vendor, and market-conversation inputs are supplied.
- Some script constants still contain the current local repo directory path `green-lappe-properties`; those should only change if the repo folder is renamed.
- The TaskNotes field schema is a draft and should be checked against the user's installed TaskNotes plugin settings before operational use.
- The global process needs critique from the user before it becomes the standard for all current and future vault/project task management.

# Lessons learned

- The rename needed both title-case text replacement and lowercase slug cleanup; active filenames and dataset references needed separate handling.
- TaskNotes should become the source of durable task truth, but the current package stopped at the project/process layer because `/pass` interrupted before task-note generation.
- Future rename work should explicitly distinguish brand name, Obsidian aliases, active filenames, historical archive names, and actual local filesystem paths.

# State

- Branch: `codex/green-property-tasknotes-workflow`
- Base commit before this session's commit: `cf8ecbf`
- PR URL: pending
- Validation: `node "$(git rev-parse --show-toplevel)/.claude/evaluate-ia-change.mjs" --staged` passed for 181 changed files before this passoff was added.

# Next actions

1. Create the `tasks/` folder under `docs/launch/projects/2026-05-24-green-property-management-tasknotes-repository/`.
2. Create the individual TaskNotes task files linked from `2026-05-24-green-property-management-project-note.md`.
3. Add TaskNotes frontmatter, status tags, dependencies, due dates, evidence links, session-log sections, decisions, and verification gates to each task note.
4. Re-run the IA validator after adding the task notes.
5. Ask the user to critique both the draft task set and the draft TaskNotes/Tasks operating process.
6. After critique, tighten the global workflow into a reusable per-project pattern for all Obsidian vaults and developer workflows.

# Do not do

- Do not invent legal, licensing, brokerage, insurance, trust-accounting, account-owner, vendor, counsel, or discovery-call decisions.
- Do not mark externally blocked launch work complete without linked evidence.
- Do not scatter durable tasks outside the project-scoped TaskNotes repository once task notes are created.
- Do not commit sensitive evidence, credentials, registrar data, legal memos, insurance documents, license documents, payment details, recovery codes, or personal registrant data.
- Do not rename historical archives or prior passoff files just to remove old project names.
- Do not change absolute local repo paths unless the repo directory is actually renamed.
