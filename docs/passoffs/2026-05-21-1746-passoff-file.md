# Summary

Updated the repository README with current operating status, root agent-entrypoint documentation, and branch hygiene guidance. Added project-local agent instruction files for Claude, Codex, Warp, and Gemini, with `AGENTS.md` as the canonical project instruction file. Updated the IA plan and evaluator so root agent files are valid durable project files. Branch cleanup is limited to stale local and remote refs already merged into `main`.

# Files changed

- Modified: `README.md`
- Modified: `.claude/information-architecture.md`
- Modified: `.claude/evaluate-ia-change.mjs`
- Created: `AGENTS.md`
- Created: `CLAUDE.md`
- Created: `WARP.md`
- Created: `GEMINI.md`
- Created: `docs/passoffs/2026-05-21-1746-passoff-file.md`

# Key decisions

- Made `AGENTS.md` the canonical project-local agent file so all supported agents share one source of truth.
- Kept `CLAUDE.md`, `WARP.md`, and `GEMINI.md` as thin entrypoints to avoid instruction drift.
- Expanded the IA validator's allowed root files because agent entrypoints are durable project files.
- Scoped branch cleanup to branches whose tips are already merged into `main`.

# Risks and open questions

- Launch readiness and operations backlog items remain blocked on owner, broker, counsel, account-owner, vendor, or market-conversation input.
- No domains, DNS, hosting, public launch content, credentials, legal-sensitive evidence, or sensitive screenshots were touched.
- Branch cleanup should not delete unmerged branches without explicit user approval.

# Lessons learned

- Root-level agent instruction files need to be represented in both README orientation and the IA validator; otherwise the project's own pre-commit hook rejects the instruction update.

# State

- Branch: `codex/update-readme-branch-cleanup`
- Commit SHA: `e433d23`
- PR URL: https://github.com/klappe-pm/green-lappe-properties/pull/30

# Next actions

1. Stage README, agent files, IA updates, and this passoff.
2. Run the staged IA evaluator and commit without bypassing hooks.
3. Push the branch, open a PR, record the PR URL in this passoff, and merge only if GitHub allows it.
4. Sync local `main` after merge.
5. Delete only local and remote branches already merged into `main`.

# Do not do

- Do not delete unmerged local or remote branches without explicit approval.
- Do not treat launch readiness and operations backlog items as complete without the required outside inputs.
- Do not buy domains, configure DNS, connect hosting, publish production content, or commit credentials, receipts, personal registrant data, payment details, recovery codes, counsel memos, license documents, or sensitive screenshots.
- Do not use `--no-verify`, force-push, reset, or bypass branch protections.
