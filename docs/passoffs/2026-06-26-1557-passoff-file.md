# 2026-06-26-1557-passoff-file

## Summary

Extracted the Green Property Management Astro site from the PPI corpus into a standalone project at `/Users/kevinlappe/Projects/green-lappe-properties`. Connected the project to `https://github.com/klappe-pm/green-lappe-properties`, pushed an initial orphan extraction branch, then created the reviewable common-history branch `codex/standalone-site-sync` and opened draft PR #69. Verified the standalone app locally with install, lint, typecheck, unit/component/static tests, and production build. This passoff records that PR #69 is intentionally draft because it replaces the existing docs/research corpus with a root-level website repo.

## Inherited state

### Open PRs

- #69 `[codex] sync standalone Green Lappe site project` | `codex/standalone-site-sync` | draft | created 2026-06-26 | https://github.com/klappe-pm/green-lappe-properties/pull/69 | <- this session

### Carry-forward from prior passoffs

_none_

### Prior passoff chain

_none in this extracted standalone clone_

## Files changed

### Created

- `docs/passoffs/2026-06-26-1557-passoff-file.md` - session handoff for the standalone-site sync.
- `/Users/kevinlappe/Projects/green-lappe-properties/` - local standalone extraction repo, committed at `b6a96c0`.
- `/Users/kevinlappe/Projects/green-lappe-properties-sync/` - common-history linked worktree for reviewable PR #69.

### Modified

- `README.md` - replaced corpus README with standalone website project instructions.
- `.github/workflows/deploy-pages.yml` - changed deployment from nested app paths to root-level app paths and copies `brand-book/*.html` into `dist/brand-book/`.
- `.github/workflows/site-ci.yml` - changed CI from nested `src/green-pm-site` paths to root-level app paths.
- `package.json` and `package-lock.json` - renamed package from `green-pm-site` to `green-lappe-properties`.
- Site source/config/test paths moved from `src/green-pm-site/` to the repository root layout.

### Deleted

- Prior documentation/research corpus paths from the sync branch, including `.claude/`, `.githooks/`, `docs/`, `product/`, and `prompts/`, except this new `docs/passoffs/` handoff.

## Key decisions

- Used a common-history branch for PR #69 because GitHub rejected the orphan extraction branch as having no history in common with `main`.
- Kept PR #69 as draft because the change intentionally removes the old product/docs corpus and should not merge without explicit confirmation.
- Moved brand-book HTML to root `brand-book/` and copied it during deploy so static tests do not treat legacy exported HTML as Astro layout pages.
- Did not delete the orphan remote branch `codex/standalone-site-extract`; deleting unmerged remote branches is destructive cleanup and needs explicit user approval.

## Risks and open questions

- PR #69 is a large repo-shape replacement: 502 files changed, mostly deleting the old corpus. Confirm that `klappe-pm/green-lappe-properties` should become the standalone website repo before marking the PR ready or merging.
- Local `green-lappe-properties` primary worktree is on an orphan `main` tracking `origin/codex/standalone-site-extract`, not `origin/main`. Treat it as an extraction artifact until cleaned up deliberately.
- The GitHub CLI reports an invalid active token for account `klappe-pm`; GitHub connector calls worked for PR creation, and `git push` worked with network escalation, but future `gh` operations may need `gh auth login`.
- Remote `main` moved after local `origin/main` was fetched; fetch before any merge or final PR decision.

## Workspace sweep

### Auto-resolved

- Folded this passoff into the active PR branch `codex/standalone-site-sync`.

### Needs user action

- Orphan extraction branch/worktree remains: inspect with `git -C /Users/kevinlappe/Projects/green-lappe-properties status -sb`; then either keep as the standalone local sandbox, or after PR #69 is accepted/rejected clean it up with explicit branch/worktree removal.
- Remote branch `codex/standalone-site-extract` has no PR and no shared history with `main`: inspect with `git ls-remote --heads origin codex/standalone-site-extract`; delete only if intentionally abandoned.
- GitHub CLI token is invalid: inspect with `gh auth status`; re-authenticate with `gh auth login -h github.com` if future PR/check/merge commands should use `gh`.
- Draft PR #69 must be reviewed before merge: inspect https://github.com/klappe-pm/green-lappe-properties/pull/69 and confirm whether the old docs/research corpus should be removed.

## Lessons learned

- GitHub cannot open a PR from an orphan branch with no common history; use a common-history branch for reviewable repo-shape replacement PRs.
- Static exported HTML in `public/` enters the Astro static quality test surface; keep legacy exports outside `public/` and copy them into `dist/` during deploy when they should be published but not tested as Astro pages.

## State

- Branch: `codex/standalone-site-sync`
- Commit SHA: 47b51ac43c3f98694cb5d01c08f1dbeebc29871f
- PR URL: https://github.com/klappe-pm/green-lappe-properties/pull/69
- Passoff version: v1

## Next actions

1. Review draft PR #69 and decide whether `klappe-pm/green-lappe-properties` should become the standalone website repository.
2. If yes, mark PR #69 ready, wait for required checks, then merge only after confirming the corpus deletion is intended.
3. If no, close PR #69 and decide whether to keep the standalone local repo at `/Users/kevinlappe/Projects/green-lappe-properties` or move it to a new GitHub repository.
4. After the PR decision, clean up the orphan extraction branch `codex/standalone-site-extract` only with explicit approval.
5. Re-authenticate `gh` if future GitHub CLI operations are needed.

## Do not do

- Do not merge PR #69 while it is draft.
- Do not delete `codex/standalone-site-extract` or its local worktree without explicit user approval.
- Do not force-push or replace `main`.
- Do not configure DNS or connect `greenpmpnw.com` without user direction.
