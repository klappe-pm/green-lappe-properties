# 2026-06-26-1610-passoff-file

## Summary

Reviewed the downloaded Green PM design reference files and turned them into an enforceable project design-system contract. Added root-level `DESIGN.md`, a JavaScript token contract for future components, and a CI-backed `npm run design:check` gate. The new checker enforces token use, audience declarations, blocked vocabulary, BaseLayout usage, and Clay CTA constraints; existing site violations were corrected so the gate passes.

## Inherited state

### Open PRs

- #69 `[codex] sync standalone Green Lappe site project` | `codex/standalone-site-sync` | draft | created 2026-06-26 | https://github.com/klappe-pm/green-lappe-properties/pull/69 | <- this session

### Carry-forward from prior passoffs

- `[2026-06-26-1557-passoff-file.md]` Review draft PR #69 and decide whether `klappe-pm/green-lappe-properties` should become the standalone website repository.
- `[2026-06-26-1557-passoff-file.md]` Do not merge PR #69 while it is draft.
- `[2026-06-26-1557-passoff-file.md]` Do not delete `codex/standalone-site-extract` or its local worktree without explicit user approval.
- `[2026-06-26-1557-passoff-file.md]` Do not force-push or replace `main`.

### Prior passoff chain

- `2026-06-26-1557-passoff-file.md`

## Files changed

Created:

- `DESIGN.md`
- `scripts/check-design-system.mjs`
- `src/design-system/gpm-design-tokens.js`
- `docs/passoffs/2026-06-26-1610-passoff-file.md`

Modified:

- `.github/workflows/site-ci.yml`
- `package.json`
- `src/components/Button.astro`
- `src/components/Header.astro`
- `src/components/forms/Field.astro`
- `src/features/rentals/ListingCard.astro`
- `src/pages/about.astro`
- `src/styles/green-pm-tokens.css`
- `tailwind.config.js`
- `docs/passoffs/2026-06-26-1610-passoff-file.md`

## Key decisions

- Preserved the repo's existing v3 CSS token values instead of replacing them with the downloaded files; the downloaded files now inform the enforceable contract and future JS component API.
- Added `DESIGN.md` as the canonical style guide and linked it to concrete enforcement through `npm run design:check` and CI.
- Added `src/design-system/gpm-design-tokens.js` as a future-facing component token contract while leaving component implementation for later, per user direction.
- Kept Clay usage intentionally narrow to CTA surfaces and token definitions, matching the "Clay = click" rule from the downloaded design reference.

## Risks and open questions

- Component implementations are intentionally stubbed for later; the token contract exists, but no component library has been added yet.
- The new checker enforces deterministic text and token rules, but visual QA still depends on future screenshot/browser verification as UI components are added.
- Existing draft PR #69 remains draft and should not be merged until the user is ready to advance the larger standalone-site work.

## Workspace sweep

### Auto-resolved

_none_

### Needs user action

- Primary checkout `/Users/kevinlappe/Projects/green-lappe-properties` is clean, but local branch `main` tracks `origin/codex/standalone-site-extract` instead of `origin/main`. Before using that checkout as the bootstrap/default checkout, review with `git -C /Users/kevinlappe/Projects/green-lappe-properties status -sb` and decide whether to reset upstream tracking.
- Draft PR #69 remains open: inspect https://github.com/klappe-pm/green-lappe-properties/pull/69 and decide when the standalone-site sync should leave draft.

## Lessons learned

The downloaded design files were largely aligned with the repo's current v3 primitives, so the safer move was to enforce and document the existing site token layer rather than overwrite live tokens from a template.

## State

- Branch: `codex/standalone-site-sync`
- Commit SHA: `c40d38a81b423fb0869aa3581148f66ce911f0be`
- PR URL: https://github.com/klappe-pm/green-lappe-properties/pull/69
- Passoff version: v2

## Next actions

1. Keep PR #69 as draft until the user is ready to review and advance the full standalone-site sync.
2. When adding future components, import from `src/design-system/gpm-design-tokens.js` and keep implementation aligned with `DESIGN.md`.
3. Run `npm run design:check`, `npm run lint`, `npm run check`, `npm test`, `npm run test:components`, `npm run test:static`, and `npm run build` before marking design-system work ready.
4. Decide whether the primary checkout's local `main` should be re-pointed to `origin/main` before future bootstrap work.

## Do not do

- Do not merge PR #69 while it is draft.
- Do not delete `codex/standalone-site-extract` or its local worktree without explicit user approval.
- Do not force-push or replace `main`.
- Do not add a component library until the user provides or approves the component set.
