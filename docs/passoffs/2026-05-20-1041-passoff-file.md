# Summary

Cleaned the remaining Green Lappe repo hygiene issues after the Phase 1 triage merge. Restored the local-only Obsidian config churn to the committed state, deleted the merged stale local `codex/phase1-green-triage` branch, moved root-level PowerPoint exports into `docs/opportunities/decks/archive/`, renamed backlog files to stable lowercase paths, rewrote the backlog metadata/content for Green Lappe Properties, updated README layout/workflow references, and regenerated the opportunity corpus index.

Ignored `.DS_Store` files were removed from disk. The cleanup keeps the exported decks for review rather than deleting them, but they no longer clutter the repository root.

# Files changed

- Updated `README.md`
- Renamed `docs/backlog/Backlog copy.md` to `docs/backlog/backlog.md`
- Renamed `docs/backlog/Backlinks Backlog.md` to `docs/backlog/backlinks-backlog.md`
- Updated `docs/backlog/backlog.md`
- Updated `docs/backlog/backlinks-backlog.md`
- Updated `docs/opportunities/README.md`
- Updated `docs/opportunities/_tools/build-corpus-index.mjs`
- Regenerated `docs/opportunities/corpus-index.json`
- Regenerated `docs/opportunities/corpus-manifest.tsv`
- Regenerated `docs/opportunities/corpus-pointer-index.md`
- Moved root deck exports into `docs/opportunities/decks/archive/`
- Created `docs/passoffs/2026-05-20-1041-passoff-file.md`

# Key decisions

- Treated the latest user instruction to clean everything as approval to discard the remaining local-only Obsidian config changes.
- Preserved all tracked deck exports but moved them under `docs/opportunities/decks/archive/` until a canonical deck is selected.
- Kept `docs/status/`, `docs/plans/`, and `docs/passoffs/` as current docs-native workflow folders.
- Kept `project-management/status-updates/` as legacy history rather than moving historical files in this pass.
- Kept generated opportunity indexes in sync with the renamed backlog files and current docs layout.

# Risks and open questions

- The archive still contains multiple deck variants; a future content cleanup should select one canonical partner deck and remove or archive superseded variants more aggressively.
- `project-management/status-updates/` remains legacy history; decide later whether to migrate it into `docs/status/`.
- Historical passoffs and status reports still mention older paths because they record prior state at the time they were written.

# State

- Branch: `codex/repo-cleanup`
- Commit SHA: `9268e5d74eaaf072c6f74547eca468bbbd1886bf`
- PR URL: `https://github.com/klappe-pm/green-lappe-properties/pull/6`

# Next actions

1. Commit this repo cleanup branch.
2. Push `codex/repo-cleanup`.
3. Open a PR against `main`.
4. Merge after GitHub reports the PR is clean and no checks or review blockers remain.
5. Prune local and remote stale branch refs after merge.
6. Verify `main` and `origin/main` match and `git status -sb` is clean.

# Do not do

- Do not delete archived deck variants until the canonical deck is selected.
- Do not rewrite historical passoffs to remove old-path references; add newer passoffs instead.
- Do not use broad TAM/SAM/SOM as proof-sprint operating capacity.
