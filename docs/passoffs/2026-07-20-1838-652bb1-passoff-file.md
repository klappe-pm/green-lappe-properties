# 2026-07-20-1838-652bb1-passoff-file

> [!warning] TRUST: NOT-TRUSTED
> This passoff and the work it describes are labeled not-trusted by operator direction. The analysis rests on unvalidated assumptions (chiefly California collectible tuition), was produced across a long multi-context session with several mid-course corrections, and has had only one independent verification pass on the deck numbers. Treat every conclusion as provisional and review the source docs before acting. Nothing here is pushed or merged.

## Summary

Produced the Green-Lappe residence-to-childcare conversion analysis for six counties and consolidated it into `docs/needs-review/`. The core deliverable is a 15-slide decision deck combining two pricing lenses (market pricing and a $2,000 / $1,500 host price card with an X-to-Z capacity ramp) with a reconciled verdict and supporting analysis, plus a Washington-to-California licensing and residence-to-facility conversion brief. Verdict: Washington (King, Snohomish) launches under both lenses; California is a pricing test, not a market decision, and only works if a provider can collect roughly $1,500 to $2,000 per child per month. All work is on branch `strategy/childcare-operator-model`, committed, unpushed, unmerged.

## Trust and provenance caveats

- The California "reopens on paper" result (Lens B) depends on a host card priced 25 to 60 percent above prevailing California family-child-care rates. Collectibility is unproven; Riverside (best paper NPV) carries the most risk.
- The numbers derive from two Python models recovered from another session's scratchpad (`b3888cd5` scratchpad), not authored in this session. They were re-run and reproduce the pasted figures, and one independent agent verified the deck against them, but the models' own inputs (tuition escalation, provider-continuity haircut, two-principal capacity) remain assumptions from prior research.
- The CA conversion brief is regulatory research, not legal advice; it ports the harness CA licensing brief (`2026-07-16-california-home-based-child-care-platforms`) and flags CDSS written classification and California counsel as prerequisites.
- This session also touched an unrelated harness repo (PR #793); that work is parked and is not part of this passoff. See "Do not do."

## Inherited state

### Open PRs

- None opened in green-lappe this session. Prior draft PR #69 (standalone-site sync) status unchanged and not touched.

### Concurrent sessions

- A live sibling session shares the green-lappe primary working tree on `main` (the worktree guard fired and redirected this session into a linked worktree). Do not edit the primary tree; this session's owner worktree is `/Users/kevinlappe/Projects/_worktrees/green-lappe-properties/childcare-strategy-claude`.

## Files changed

### Created (all under docs/needs-review/)

- `README.md` reading order and open decisions for the folder.
- `ca-residence-to-childcare-conversion-brief.md` the WA-to-CA regulatory port (new this session).
- `green-lappe-combined-deck.html` the 15-slide two-lens decision deck (also published as an Artifact: https://claude.ai/code/artifact/ec65dddb-c4d3-402e-80c1-ce9610f523ba).
- `model-inputs-consolidated.md` the input register (copied from prior research).
- `models/green_lappe_model_v2.py` (market pricing) and `models/green_lappe_model_v3.py` (host card + ramp).

### Moved into docs/needs-review/

- `childcare-operator-property-model-strategy.md` and `childcare/` (six plan docs), from `docs/plans/`.
- `orange-county-childcare-tam.md`, from `docs/research/` (which was removed).

## Key decisions

- Consolidated all childcare analysis into a single `docs/needs-review/` folder per operator direction, so everything is reviewed together before any permanent placement.
- Kept the two model filenames in underscore form because the deck's provenance slide cites them by that exact name.
- Wrote the CA brief as a port of the existing harness research rather than fresh research, to avoid inventing regulatory claims.
- Restored the Orange County TAM from a sweep stash (`6826ebe^3`) under a compliant name rather than leaving it as the stashed `docs/backlog/Untitled.md`.

## Risks and open questions

- California go or no-go is unresolved and rests entirely on a tuition survey (Newport Beach, Irvine, Costa Mesa, Riverside). Nothing else moves the verdict as much.
- The host price card should be treated as a floor, not a ceiling; in Washington market tuition exceeds it, so pricing at the card underprices the premium.
- The capacity ramp destroys value in its solo phase; the design target is licensing at eight children with the shortest legal solo phase, which needs confirmation against CA small-home age-mix rules per property.
- The two-principal 150 to 250 door operating capacity is the central unvalidated assumption carried from prior research into the platform ramp.

## Workspace sweep

### Needs user action

- Decide whether to push `strategy/childcare-operator-model` and open a review PR, or leave it local.
- Review `docs/needs-review/` and confirm which artifacts graduate to permanent locations (for example the strategy into `docs/plans/`, the TAM into research) versus staying provisional.
- Resolve the concurrent-session ownership of the green-lappe primary tree before any further edits there.

## Lessons learned

- Repeated "which project" questions in place of reading the project first drew justified operator frustration; orienting by reading the actual research corpus (recovered docs, vault, harness research) is the correct first move, not asking.
- The real research corpus for this project lived outside the website repo (git history, another session's scratchpad, the harness `!research` tree, the trust-documents vault); a project can be data-rich while its current repo looks empty.

## State

- Branch: `strategy/childcare-operator-model`
- Head commit: `652bb14`
- Base: `origin/main` (two commits ahead, unpushed)
- Worktree: `/Users/kevinlappe/Projects/_worktrees/green-lappe-properties/childcare-strategy-claude`
- Trust: NOT-TRUSTED
- Passoff version: v1

## Next actions

1. Review `docs/needs-review/README.md`, then the deck, then the CA brief.
2. Run the California tuition survey before any California commitment; it is the single gating input.
3. Decide push-and-PR versus keep-local for `strategy/childcare-operator-model`.
4. Decide graduation of each `docs/needs-review/` artifact to a permanent location once trusted.

## Do not do

- Do not treat any California NPV as decision-ready; it is conditional on the unrun tuition survey.
- Do not edit the green-lappe primary working tree while the sibling session holds it.
- Do not push or merge this branch without explicit operator direction.
- Do not conflate this with the parked harness PR #793; that is a separate repo and a separate decision.
