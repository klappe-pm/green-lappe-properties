# Summary

Codified the Green Lappe archive rule: files inside underscore-prefixed folders
are non-live, changed non-live paths are logged and ignored by the IA evaluator,
and live Markdown cannot depend on `docs/_*` material. Removed active
references to the retired opportunity corpus indexer and archived pitch/deck
sources. The latest prior passoff was amended with the non-live archive
decision and the future diagram-migration requirement.

# Files changed

- Modified: `.claude/evaluate-ia-change.mjs`,
  `.claude/information-architecture.md`, `README.md`.
- Modified: `docs/backlog/backlog.md`, `docs/backlog/codex-index-model.md`,
  `docs/status/2026-05-20-status-report.md`,
  `docs/uxr/research-goals.md`.
- Modified: `docs/passoffs/2026-05-22-1234-passoff-file.md`.
- Created: this passoff.

# Key decisions

- Archived opportunity corpus tooling stays ignored as non-live material.
- Any future corpus index rebuild must be a new active-zone tool, not a revived
  dependency on archived deck tooling.
- Active docs must not use archived files as dependencies, blockers, or source
  of truth inputs.

# Risks and open questions

- Older archived notes may still contain historical stale paths. Preserve them
  as history unless a future task promotes a specific archived artifact back
  into active use.
- Launch readiness and operations work remain blocked on user-supplied owner,
  broker, counsel, account-owner, vendor, or market-conversation input.
- Future diagram migration still needs a detailed plan before moving active
  diagram sources or outputs.

# Lessons learned

- An archive rule needs both behavior and evidence: ignore non-live paths for
  IA requirements, fail live dependencies on archives, and log ignored archive
  changes so reviewers can see what happened.

# State

- Branch: `codex/archive-and-docs-rules`
- Commit SHA: `81499401326ce387087b6825cb8dfaba9c1b7534`
- PR URL: https://github.com/klappe-pm/green-lappe-properties/pull/36

# Next actions

1. Run `node "$(git rev-parse --show-toplevel)/.claude/evaluate-ia-change.mjs" --staged`
   after staging Green Lappe changes.
2. Plan the diagram migration before moving sources to
   `docs/plans/diagrams/` or rendered outputs to `docs/diagrams/`.
3. Keep launch/legal/ops items blocked until the user supplies the required
   external decisions and evidence.

# Do not do

- Do not rebuild or depend on archived corpus indexer tooling unless a future
  task explicitly creates a new active-zone tool.
- Do not treat UXD diagrams as legal, licensing, insurance, trust-accounting,
  privacy, cybersecurity, tax, records-retention, or public-launch approval.
- Do not buy domains, configure DNS, connect hosting, publish production
  content, or commit credentials, receipts, personal registrant data, payment
  details, recovery codes, counsel memos, license documents, or sensitive
  screenshots.
- Do not use `--no-verify`, force-push, reset, or bypass branch protections.
